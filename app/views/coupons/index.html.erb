<h1>Coupon List</h1>

<ul>
  <% @coupons.each do |q| %>

  <li>
    <%= link_to "#{q.id}. #{q.store} --- #{q.coupon_code}", coupon_path(q.id) %>
  </li>
  <% end %>
</ul>
<br>
<%= link_to "Create a Coupon", new_coupon_path %>
